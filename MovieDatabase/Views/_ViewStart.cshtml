@*
    _ViewStart.cshtml - Runs before every view is rendered.
    Sets the default layout to _Layout.cshtml so all pages share
    the same navigation, header, footer, and styling.
*@
@{
    Layout = "_Layout";
}
